# Autonomous QA/Fix Progress — 2026-05-20

## Goal
System flawless enough for real Instagram DM → appointment → CRM path.

## Fixes implemented locally

1. Suggested-slot acceptance fix
   - User selects bot-suggested slot like `11:00 uygun` after unavailable `15:00`.
   - FSM now treats suggested-slot selection as appointment creation input.
   - Keeps unavailable requested time until user selects alternative; selected slot can overwrite it.
   - Prevents final confirmation without DB appointment creation.

2. IG DM contact-channel handling
   - `igdm`/`instagram_dm` preserved instead of collapsing all to `instagram_private_api`.
   - Real numeric IG sender can use Instagram DM as contact method.
   - Synthetic/non-real tests still require explicit phone unless date/time present.

3. Missing-field helper restored
   - `check_missing_fields()` now reports missing fields and create/update capability.

4. Date/time extraction hardening already in diff
   - Avoid bare numeric/name false time extraction unless message has scheduling context.

## Local regression

Command:
`cd C:/Users/oyunc/Desktop/instagram-randevu-bot/booking-api && python -m pytest tests -q`

Result:
`462 passed, 35 skipped, 2 warnings`

Artifact:
`test-results/autonomous-qa/pytest-pass.log`

## Prod probes

Artifacts:
- `test-results/autonomous-qa/api-inventory-and-probes.log`
- `test-results/autonomous-qa/prod-deep-matrix.json`

Current prod is still old code:
- live bug still visible on prod conversation `67000808415`: `collect_name`, `collecting`, `appointment_id=null`, requested_time old `15:00`.
- local code now fixed; prod needs deploy before live retest.

## Known remaining work

Next autonomous loop:
1. Run local server with fixed code.
2. Run local deep matrix against `http://localhost:18000` or start if needed.
3. CRM UI/browser smoke.
4. n8n workflow validation + webhook replay from workflow JSON.
5. Security/rate-limit/malformed payload matrix.
6. TestSprite rerun after local server fixed.
7. Deploy only if user/prod process allows; then real IG retest selected-slot bug.

## Do not forget

- No secret printing.
- Ask before destructive prod DB cleanup.
- Ask before extra real IG live sends beyond controlled retest.
