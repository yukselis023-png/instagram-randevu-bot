# Autonomous QA/Fix Status — 2026-05-20 21:56 GMT+3

## Goal
System correctness for Instagram DM → AI reply → booking/CRM persistence.

## Fix wave A completed locally

Changed:
- `booking-api/app/generic_core.py`
  - stale `collect_name` + existing name + slot/time reply now treated as datetime/slot selection
  - suggested slot selection can create appointment even if LLM intent/reply is question-like
  - unavailable requested time is preserved so next suggested-slot reply can overwrite it
  - false confirmation path now blocked by persistence requirement already covered by invariant tests
  - raw inbound log now preserves original platform and adds `normalized_platform`
  - Instagram DM contact fallback tuned: real numeric IG sender can use DM as contact after name, explicit `collect_phone` still asks phone
- `booking-api/app/pipeline_wrapper.py`
  - `check_missing_fields` now accepts `lead_name` as full name fallback and service from memory
- Added regressions:
  - `tests/test_live_slot_acceptance_regression.py`
  - `tests/test_live_stale_collect_name_slot_bug.py`

## Local regression result

`python -m pytest tests -q`

Result: `462 passed, 35 skipped, 2 warnings`

Artifact:
- `test-results/autonomous-qa/pytest-green-verified.txt`

## Inventory/artifacts

- Route inventory: `test-results/autonomous-qa/route-inventory.txt`
- DB schema/counts: `test-results/autonomous-qa/db-schema-counts.txt`
- n8n workflow validation: `test-results/autonomous-qa/n8n-validate.txt`
- Contract probe: `test-results/autonomous-qa/contract-probe.txt`
- Prod deep matrix: `test-results/autonomous-qa/deep-matrix-prod.json`

## Prod synthetic matrix result

Base: `https://instagram-randevu-bot.onrender.com`

Pass:
- `/health` 200
- `/version` 200
- `/api/conversations?limit=5` 200
- `/api/appointments` 200
- `/api/customers` 200
- Duplicate same `message_id`: second response `duplicate=true`, `should_reply=false`
- 12-user concurrency: all HTTP 200, replies produced, serial lock active
- malformed empty/missing text: ignored safely with 200 for partially valid payloads; schema-invalid payloads return 422
- n8n workflow JSON validation OK

Issues found:
1. Prod does NOT yet include local fixes. Version: `c9d8196358e9cf2bb6d26c1cc3c8e360adb2736c`.
2. Mojibake Turkish chars in matrix output (`tasar�m`, `�n g�r��me`) — likely terminal/output encoding or response charset path; needs browser/API charset verification.
3. Synthetic messages containing numbers in text (`kullanıcı 5`) can be over-extracted as time/date in some conversations. Needs extraction hardening.
4. Local contract probe without DB env throws `RuntimeError('No database URL configured')` for DB endpoints. Expected in bare TestClient env, but should be made test-friendly or documented.
5. Live real IG critical bug fixed locally, but must deploy before re-testing real IG.

## Next wave

1. Inspect/patch numeric over-extraction.
2. Add endpoint-level API tests for CRM/customer/appointment/campaign where DB can be mocked or prod-safe.
3. Browser CRM UI smoke against prod.
4. n8n webhook live/path replay if credentials/workflow endpoint available.
5. Deploy local fixes after user/owner deployment approval if required.
6. Re-run real Instagram slot-selection live E2E after deploy.

## Update — 21:58 GMT+3

Additional fix:
- Hardened `extract_generic_datetime_time`: plain labels like `kullanıcı 5` no longer become `05:00`.
- Added regression: `tests/test_numeric_label_not_time_regression.py`.

Latest local suite:
- `463 passed, 35 skipped, 2 warnings`
- Artifact: `test-results/autonomous-qa/pytest-green-plus-numeric.txt`

Current local code is green.

Important: prod is still old until deploy. Real Instagram final persistence fix must be verified after deployment.
